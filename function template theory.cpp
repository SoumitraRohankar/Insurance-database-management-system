/*function template are generic i.e independant of the data type 
template are used mainly to ensure code reusability of the programs 
we can just create a simple function or a class thta takes data parameter and impement the code

class template behave as silimar function as function template

a class tmeplate starts with the keyword "template" followed by template parameters inside <> which
is followed by class declaration

template <class T>
class className
{
	private:
		T var;
		..
		public:
		T functionName(T arg)
		
}

#include<iostream>
using namespace std;
int main()
{
   classname<datatype>:	
}

*/



